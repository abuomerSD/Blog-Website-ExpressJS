<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/head.ejs') %>
<body>
    <%- include('./partials/nav.ejs') %>

    
        
        <% for( let i = 0; i < blogs.length; i++ ) { %>
            <div class="blogs">
                <a href= <%= `/blogs/${blogs[i]._id}` %> target="_blank" >
                    <h3><%= blogs[i].title %></h3>
                    <p><%= blogs[i].body %></p>
                </a>
            </div>
        <% } %>
         
    

    <%- include('./partials/footer.ejs') %>
</body>
</html>